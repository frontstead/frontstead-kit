import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { ListingLanding } from '@/components/listing-landing'; import { getLanding } from '@/lib/landings';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const landing = await getLanding('areas', slug); return landing ? { title: `${landing.metadata.name} Homes | ABC Realty`, description: landing.metadata.description ?? `Homes in ${landing.metadata.name}.` } : {}; }
export default async function AreaPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const landing = await getLanding('areas', slug); if (!landing) notFound(); return <ListingLanding landing={landing} kind="area" />; }
