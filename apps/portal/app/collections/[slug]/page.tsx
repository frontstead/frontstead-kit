import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { ListingLanding } from '@/components/listing-landing'; import { getLanding } from '@/lib/landings';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const landing = await getLanding('collections', slug); return landing ? { title: `${landing.metadata.name} | ABC Realty`, description: landing.metadata.description ?? `Browse ${landing.metadata.name}.` } : {}; }
export default async function CollectionPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const landing = await getLanding('collections', slug); if (!landing) notFound(); return <ListingLanding landing={landing} kind="collection" />; }
