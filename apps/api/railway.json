{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build:api"
  },
  "deploy": {
    "preDeployCommand": ["npm run db:migrate && if [ -n \"$TYPESENSE_HOST\" ]; then npm run typesense:migrate --workspace=api; else echo \"Typesense not configured; skipping schema migration\"; fi"],
    "startCommand": "npm run start:api",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
